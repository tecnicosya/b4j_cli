B4J=true@Version=9.90@End=Sub Process_GlobalsEnd SubSub AppStart (Args() As String)    If Args.Length = 0 Then        PrintUsage        Return    End If    Dim parser As CommandParser    parser.Initialize    Dim command As CommandData = parser.Parse(Args)    If command.IsValid = False Then        PrintError(command.ErrorMessage)        Return    End If    Select command.Command        Case 